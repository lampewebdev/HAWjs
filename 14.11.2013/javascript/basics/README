this should be placed inside your document root

e.g. /var/www/


or symlink it with `ln -s /absolute/path/to/this/directory /var/www/HAWjs/`. open `http://localhost/HAWjs/` in your browser. 'should' work.

